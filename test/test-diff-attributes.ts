'use strict';

const Expect: Chai.ExpectStatic = require('chai').expect;
const Should: Chai.ShouldAssertion = require('chai').should();

describe('diff attributes', function() {
  describe('#diffAttributes', function() {
    it('TODO', function() {
      Expect(1).to.equal(1);
    });
  });
});
